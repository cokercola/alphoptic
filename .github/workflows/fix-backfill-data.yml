name: Fix backfill data (tickers + next_event)
on:
  workflow_dispatch: {}
permissions:
  contents: write
concurrency:
  group: backfill-bills-history
  cancel-in-progress: false
jobs:
  fix:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: pip install requests anthropic rapidfuzz
      - name: Run fix
        env:
          CONGRESS_API_KEY: ${{ secrets.CONGRESS_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          python scripts/fix_backfill_data.py
      - name: Commit updated data
        if: always()
        run: |
          git config user.name "alphoptic-bot"
          git config user.email "actions@github.com"
          git add data/bills.json data/bills-index.json data/companies.json
          [ -d data/bills-chunks ] && git add data/bills-chunks/
          git diff --staged --quiet || git commit -m "Fix backfilled company tickers and missing next_event"
          git pull --rebase --autostash origin main
          git push
